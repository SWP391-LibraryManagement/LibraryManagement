# Frontend

Approved stack: React + Bootstrap.

This folder will contain the frontend application after UI flow and API contracts are aligned with specs.

Expected structure:

```text
frontend/
  src/
```
