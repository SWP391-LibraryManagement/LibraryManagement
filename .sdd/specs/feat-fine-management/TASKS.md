# TASKS.md - FE09 Fine Management

Status: NOT STARTED

Tasks will be decomposed only after `PLAN.md` is approved.

## Prototype Drift Note

As of 2026-06-22, FE09 has prototype backend and frontend code, but these tasks have not been decomposed, reviewed, or mapped to the approved `SPEC.md`. Do not mark FE09 complete from code presence alone.

Before implementation can be accepted, the team must add concrete tasks for prototype reconciliation, server-side overdue calculation, duplicate fine prevention, paid/waived status handling, borrowing eligibility integration, authorization, traceability tags, and tests.

Expected task groups later:

- API contract tasks
- Database/schema adjustment tasks
- Fine calculation service tasks
- Duplicate fine prevention tasks
- View fine information tasks
- Record fine collection tasks
- Mark fine as paid tasks
- Borrowing eligibility integration tasks
- Notification trigger preparation tasks
- Authorization tests
- Unit/integration test tasks
- End-to-end test tasks
