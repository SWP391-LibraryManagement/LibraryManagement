name: Deploy staging

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]
  workflow_dispatch:
    inputs:
      fe10_inbox_migration_confirmed:
        description: FE10 inbox migration was applied twice and verified on staging
        required: true
        default: false
        type: boolean
      fe10_borrowing_result_templates_confirmed:
        description: FE10 borrowing result templates migration was applied twice and verified on staging
        required: true
        default: false
        type: boolean
      fe10_unicode_repair_confirmed:
        description: FE10 Unicode repair migration was applied twice and exact values were verified on staging
        required: true
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: library-staging
  cancel-in-progress: false

jobs:
  preflight:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    environment:
      name: staging
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - name: Verify FE10 inbox migration gate
        shell: pwsh
        env:
          EVENT_NAME: ${{ github.event_name }}
          MANUAL_CONFIRMATION: ${{ inputs.fe10_inbox_migration_confirmed }}
          EXPECTED_MIGRATION_SHA256: ${{ vars.FE10_INBOX_MIGRATION_SHA256 }}
        run: |
          if (
            $env:EVENT_NAME -eq 'workflow_dispatch' -and
            "$env:MANUAL_CONFIRMATION".ToLowerInvariant() -ne 'true'
          ) {
            Write-Error 'Apply and verify the FE10 inbox migration twice before manual deployment.'
            exit 1
          }

          $expectedHash = "$env:EXPECTED_MIGRATION_SHA256".Trim().ToLowerInvariant()
          if ($expectedHash -notmatch '^[0-9a-f]{64}$') {
            Write-Error 'The staging FE10 migration proof is missing or malformed.'
            exit 1
          }

          $migrationPath = 'database/migrations/2026-07-27-fe10-personal-inbox-read-state.sql'
          $utf8NoBom = [Text.UTF8Encoding]::new($false, $true)
          $migrationText = [IO.File]::ReadAllText($migrationPath, $utf8NoBom)
          $lfText = $migrationText.Replace("`r`n", "`n").Replace("`r", "`n")
          $crlfText = $lfText.Replace("`n", "`r`n")
          $lfHash = [Convert]::ToHexString(
            [Security.Cryptography.SHA256]::HashData($utf8NoBom.GetBytes($lfText))
          ).ToLowerInvariant()
          $crlfHash = [Convert]::ToHexString(
            [Security.Cryptography.SHA256]::HashData($utf8NoBom.GetBytes($crlfText))
          ).ToLowerInvariant()
          if ($expectedHash -notin @($lfHash, $crlfHash)) {
            Write-Error 'The reviewed FE10 migration does not match the staging migration proof.'
            exit 1
          }

          Write-Output 'FE10 migration proof accepted for the exact deployment head and LF/CRLF rendering.'

      - name: Verify FE10 borrowing result templates migration gate
        shell: pwsh
        env:
          EVENT_NAME: ${{ github.event_name }}
          RESULT_MANUAL_CONFIRMATION: ${{ inputs.fe10_borrowing_result_templates_confirmed }}
          EXPECTED_RESULT_MIGRATION_SHA256: ${{ vars.FE10_BORROWING_RESULT_TEMPLATES_SHA256 }}
        run: |
          if (
            $env:EVENT_NAME -eq 'workflow_dispatch' -and
            "$env:RESULT_MANUAL_CONFIRMATION".ToLowerInvariant() -ne 'true'
          ) {
            Write-Error 'Apply and verify the FE10 borrowing result templates migration twice before manual deployment.'
            exit 1
          }

          $expectedResultHash = "$env:EXPECTED_RESULT_MIGRATION_SHA256".Trim().ToLowerInvariant()
          if ($expectedResultHash -notmatch '^[0-9a-f]{64}$') {
            Write-Error 'The staging FE10 borrowing result templates migration proof is missing or malformed.'
            exit 1
          }

          $resultMigrationPath = 'database/migrations/2026-07-29-fe10-borrowing-result-templates.sql'
          $utf8NoBom = [Text.UTF8Encoding]::new($false, $true)
          $resultMigrationText = [IO.File]::ReadAllText($resultMigrationPath, $utf8NoBom)
          $resultLfText = $resultMigrationText.Replace("`r`n", "`n").Replace("`r", "`n")
          $resultCrlfText = $resultLfText.Replace("`n", "`r`n")
          $resultLfHash = [Convert]::ToHexString(
            [Security.Cryptography.SHA256]::HashData($utf8NoBom.GetBytes($resultLfText))
          ).ToLowerInvariant()
          $resultCrlfHash = [Convert]::ToHexString(
            [Security.Cryptography.SHA256]::HashData($utf8NoBom.GetBytes($resultCrlfText))
          ).ToLowerInvariant()
          if ($expectedResultHash -notin @($resultLfHash, $resultCrlfHash)) {
            Write-Error 'The reviewed FE10 borrowing result templates migration does not match the staging migration proof.'
            exit 1
          }

          Write-Output 'FE10 borrowing result templates migration proof accepted for the exact deployment head and LF/CRLF rendering.'

      - name: Verify FE10 Unicode repair migration gate
        shell: pwsh
        env:
          EVENT_NAME: ${{ github.event_name }}
          UNICODE_REPAIR_MANUAL_CONFIRMATION: ${{ inputs.fe10_unicode_repair_confirmed }}
          EXPECTED_UNICODE_REPAIR_SHA256: ${{ vars.FE10_UNICODE_REPAIR_SHA256 }}
        run: |
          if (
            $env:EVENT_NAME -eq 'workflow_dispatch' -and
            "$env:UNICODE_REPAIR_MANUAL_CONFIRMATION".ToLowerInvariant() -ne 'true'
          ) {
            Write-Error 'Apply the FE10 Unicode repair migration twice and verify exact values before manual deployment.'
            exit 1
          }

          $expectedUnicodeRepairHash = "$env:EXPECTED_UNICODE_REPAIR_SHA256".Trim().ToLowerInvariant()
          if ($expectedUnicodeRepairHash -notmatch '^[0-9a-f]{64}$') {
            Write-Error 'The staging FE10 Unicode repair migration proof is missing or malformed.'
            exit 1
          }

          $unicodeRepairMigrationPath = 'database/migrations/2026-08-03-fe10-unicode-repair.sql'
          $utf8NoBom = [Text.UTF8Encoding]::new($false, $true)
          $unicodeRepairMigrationText = [IO.File]::ReadAllText(
            $unicodeRepairMigrationPath,
            $utf8NoBom
          )
          $unicodeRepairLfText = $unicodeRepairMigrationText.Replace("`r`n", "`n").Replace("`r", "`n")
          $unicodeRepairCrlfText = $unicodeRepairLfText.Replace("`n", "`r`n")
          $unicodeRepairLfHash = [Convert]::ToHexString(
            [Security.Cryptography.SHA256]::HashData(
              $utf8NoBom.GetBytes($unicodeRepairLfText)
            )
          ).ToLowerInvariant()
          $unicodeRepairCrlfHash = [Convert]::ToHexString(
            [Security.Cryptography.SHA256]::HashData(
              $utf8NoBom.GetBytes($unicodeRepairCrlfText)
            )
          ).ToLowerInvariant()
          if ($expectedUnicodeRepairHash -notin @(
            $unicodeRepairLfHash,
            $unicodeRepairCrlfHash
          )) {
            Write-Error 'The reviewed FE10 Unicode repair migration does not match the staging proof.'
            exit 1
          }

          Write-Output 'FE10 Unicode repair migration proof accepted for the exact deployment head and LF/CRLF rendering.'

  deploy-backend:
    needs: preflight
    runs-on: ubuntu-latest
    environment:
      name: staging
      url: ${{ vars.STAGING_API_URL }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Prepare backend deployment package
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force deploy/backend | Out-Null
          Copy-Item backend/package.json,backend/package-lock.json deploy/backend/
          Copy-Item backend/src deploy/backend/src -Recurse
          New-Item -ItemType Directory -Force deploy/backend/database/migrations | Out-Null
          Copy-Item database/migrations/2026-07-22-library-metadata-compatibility.sql,database/migrations/add_change_password_otp_token_type.sql,database/migrations/2026-07-29-fe10-borrowing-result-templates.sql,database/migrations/2026-08-03-fe10-unicode-repair.sql deploy/backend/database/migrations/
          New-Item -ItemType Directory -Force deploy/backend/scripts | Out-Null
          Copy-Item backend/scripts/stagingBorrowCandidates.js deploy/backend/scripts/

      - name: Install backend production dependencies
        shell: pwsh
        working-directory: deploy/backend
        run: npm ci --omit=dev

      - name: Deploy backend
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ vars.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: deploy/backend

  deploy-frontend:
    needs: deploy-backend
    runs-on: ubuntu-latest
    environment:
      name: staging
      url: ${{ vars.STAGING_FRONTEND_URL }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci
        working-directory: frontend

      - name: Build staging frontend
        run: npm run build
        working-directory: frontend
        env:
          VITE_API_BASE_URL: ${{ vars.STAGING_API_URL }}/api

      - name: Deploy frontend
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          action: upload
          app_location: frontend/dist
          output_location: ''
          skip_app_build: true

  smoke-test:
    needs: [deploy-backend, deploy-frontend]
    runs-on: ubuntu-latest
    environment:
      name: staging
      url: ${{ vars.STAGING_FRONTEND_URL }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Verify staging endpoints
        run: npm run smoke:staging
        env:
          STAGING_FRONTEND_URL: ${{ vars.STAGING_FRONTEND_URL }}
          STAGING_API_URL: ${{ vars.STAGING_API_URL }}
